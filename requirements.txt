# Combined dependencies for the segmentation (nnUNet, SAM2), morphology
# feature extraction, and mitochondria-ER analysis pipelines in this
# repository. Tested with Python 3.10 and CUDA 12.2 on Linux.
#
# nnUNet/requirements.txt and SAM2/requirements.txt list the dependencies of
# the respective vendored third-party packages individually; this file is the
# merged, installable set for the whole repository.

torch>=2.3.1
torchvision>=0.18.1
numpy>=1.24.4
pillow>=9.4.0
tqdm>=4.66.1
hydra-core>=1.3.2
opencv-python>=4.9.0
scikit-image>=0.19.3
scikit-learn
scipy
pandas
SimpleITK>=2.2.1
tifffile
joblib
matplotlib>=3.9.0
seaborn
graphviz
requests
nibabel
dicom2nifti
imagecodecs
pynrrd
loguru
blosc2
yacs
einops
umap-learn
hdbscan
pyradiomics
acvl-utils>=0.2,<0.3
dynamic-network-architectures
batchgenerators>=0.25

# Optional, used by nnUNet for GPU-accelerated resampling / torch.compile
# speedups. Require a matching CUDA toolkit; skip if unavailable.
# triton>=2.3.0
# cupy-cuda12x
